# 更新日志

## v0.33.0 —— 首个公开测试版

第一个可以正常使用的版本。

### 内核层
- proot + Ubuntu 24.04 + Node 24 + dsh 0.1.5-rc.1，全部本地运行，无需 root
- 首次启动自动解包（进度可见），之后秒进
- 自动从系统取 DNS；启动前清理残留 dsh 进程（防端口占用）
- proot 启动目录设为容器内工作区，保证会话能挂上工作区

### 移动端适配（客户端插件）
- 三栏 Grid 收敛为单栏，中栏吃满整宽
- 侧栏变浮层抽屉：300ms 缓动、遮罩点击关闭、**左缘右滑打开 / 抽屉内左滑关闭**
- dsh 设置面板改整屏，导航变顶部横向标签条（原先在 384px 屏上内容列只剩 196px，中文被挤成一列一个字）
- 汉堡按钮适配安全区；去掉点击蓝框
- 输入栏工具行强制单行；弹层不出屏
- 亮/暗主题切换走 240ms 颜色过渡，不再硬切
- **等待遮罩**：会话创建期间（约一分钟）挡住误触，就绪后自动消失

### 原生设置页
- 外观：8 个主色预设 / 跟随系统·浅色·深色 / 会话字号 / **界面缩放 70%~120%** / 界面动效开关
- 工作区：查看当前路径与权限状态、重新选择文件夹
- 高级：进入 dsh 原生设置、查看内核日志

### 工作区
- 首次启动用**安卓原生文件夹选择器**指定工作区，绑定到容器内 `/workspace`
- 需要「所有文件访问」权限（proot 是原生进程，用不了 SAF 授权）；不授权则退回 App 私有目录
- 自动写入 dsh 工作区注册表，并清理早期版本留下的 `/root` 僵尸记录

### 修复记录（开发期）
- 侧栏改 `fixed` 后脱离 grid 流，导致中栏宽度变 0、主页整片空白
- `pointer-events` 继承导致设置浮层不可点击（表现为「滑一下闪一下然后卡死」）
- 设置入口被插件劫持后与「高级 → dsh 原生设置」形成死循环
- 移除诊断日志自动写入剪贴板（开发期临时手段，发布版不该动用户剪贴板）
